# Phase 1 — Foundation

## Goals
- Set up project structure (monorepo layout)
- PostgreSQL + pgvector database with schema
- FastAPI backend with health check
- Docker Compose orchestration
- Basic CI configuration

## Deliverables
- `docker-compose.yml`, Dockerfiles, `.env`
- Database init scripts (extensions, schema, indexes)
- Backend skeleton with `/health` endpoint
- Configuration management via `pydantic-settings`

## Status: ✅ Complete
